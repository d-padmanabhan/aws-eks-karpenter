apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: todo-app
type: Opaque
stringData:
  # Generate a secure secret key: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
  DJANGO_SECRET_KEY: "change-me-to-a-secure-random-key-in-production"
