apiVersion: v1
kind: ServiceAccount
metadata:
  name: karpenter
  namespace: karpenter
  annotations:
    # This will be populated from Terraform output
    # Replace with actual Karpenter controller role ARN after terraform apply
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/todo-app-cluster-karpenter-controller
